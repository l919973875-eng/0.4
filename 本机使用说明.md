# 本机使用说明

## 最简单的使用方法

双击项目根目录的 `启动舆情监测.cmd`。浏览器会自动打开本机控制页（`127.0.0.1`），无需输入命令。

- **运行全量巡航**：采集公开全球新闻、RSS 和已配置的公开来源，形成 48 小时原始新闻池并进行涉华/领域初筛。
- **运行社交扫描**：使用你本机已登录的专用 Chrome 读取公开页面；不会上传 Cookie、密码或私信。首次运行如弹出专用浏览器，只需自行完成登录。
- **刷新页面**：不重新采集，只用已有数据重建事件和网页。
- **查看结果**：双击 `查看结果.cmd`，或在控制页中直接查看“原始资料 / Raw”。

## 不要直接双击 index.html

`site/index.html` 会读取同目录的数据文件。直接以 `file://` 打开时，浏览器会拦截这类读取并显示 `Failed to fetch`。请始终通过两个 CMD 启动器进入控制页。

## 本地数据位置

- `data/raw_news_48h.json`：所有去重后的 48 小时原始新闻及链接。
- `data/run_status.json`：最近一次运行状态与错误摘要。
- `data/local_dashboard_runner.log`：由控制页启动的运行日志。

控制页只绑定 `127.0.0.1`，外部网络无法访问。