# v0.4.4：小红书苗头 Query 自动生成器

这版不改现有 GitHub Actions / GitHub Pages / JSON 数据链，只升级“小红书发现层”。

## 为什么改

v0.4.3 的 Review 与社交精筛已经能识别很多风险动作，但小红书入口仍只有 20 条固定 signal 搜索词。结果是：后端会判断“绑架、欠薪、搜查、反倾销、驱逐、导弹、供应链中断”等苗头，但前端搜索层可能根本没有把相关帖子抓回来。

v0.4.4 让搜索层和筛选层使用同一份 taxonomy。

## 新结构

```text
config/social_keywords.yaml
        │
        ├─ taxonomy
        │   ├─ objects
        │   ├─ actions
        │   └─ scenes
        │
        ├─ high-value templates
        │
        └─ query budget / pinned baseline
                ↓
tools/social_keyword_pack.py
                ↓
36 条 daily signal / 72 条 manual broad
                ↓
MediaCrawler → signals_external.json → Review → signals.json → events.json
```

## 不是全量笛卡尔积

不会做：

```text
所有对象 × 所有动作 × 所有场景
```

只做语义上合理的模板，例如：

```text
人员 × 人员安全动作
中资 + 项目场景 × 项目异常
工程央企 × 工程场景 × 项目异常
华为/中兴 × 科技监管动作
比亚迪/宁德时代 × 汽车/电池监管动作
TikTok/字节 × 平台监管动作
Temu/Shein × 电商监管动作
台海/南海 × 军事动作
南海热点 × 中国海警/军舰 × 海上安全动作
供应链对象 × 对应中断动作
```

## signal

默认小红书每天最多 36 条。

其中 8 条为常驻基线，其余按模板配额生成。选择时同时考虑：

- 模板 priority；
- 对象/动作自身 weight；
- 不同对象的覆盖；
- 不同异常动作的覆盖；
- 北京时间日期稳定轮换；
- 自动去重。

同一天重复生成结果一致；换一天会轮换一部分长尾 query。

## broad

`broad` 仅建议手动运行，默认最多 72 条。它保留少量“中国 / 中资 / 华为 / 台海”等泛入口，同时扩展更多项目、地区、企业和风险组合。

## Review 同步

`cloud_runner.py` 会从 `config/social_keywords.yaml` 自动读取：

- `taxonomy.objects` → 社交涉华对象；
- `taxonomy.actions` → 异常/风险动作；
- `taxonomy.scenes` → 战略场景。

因此以后增加一个新的 canonical 风险动作时，不需要再同时修改 `cloud_runner.py` 的另一套硬编码词表。

原有英文/历史 fallback 规则仍保留，避免配置读取失败时整个 Review 失效。

## 抖音 / 微博

本版**没有直接把生成器打开到抖音、微博**。

原因是小红书已经跑通，应该先用真实 Review 数据校准 query budget、模板噪声和命中率。`dy / wb` 继续使用 v0.4.3 静态词包，不影响现有行为。

确认小红书稳定后，只需要把平台加入 `generator.enabled_platforms` 并配置各自预算/权重，即可复制逻辑。

## 诊断

社交 workflow 现在会先运行：

```bash
python tools/test_social_keyword_pack.py
```

如果 taxonomy、预算、去重或轮换逻辑损坏，会在下载/运行 MediaCrawler 前失败。

每次非自定义关键词运行还会生成：

```text
data/mediacrawler_xhs_keyword_plan.json
```

该文件只上传到 `social-crawl-diagnostics-*` Artifact，不提交仓库。里面包含当天实际 query、来源模板、score、候选池数量和各模板选中数量。

## 本地检查

```bash
python tools/social_keyword_pack.py --platform xhs --pack signal --field count
python tools/social_keyword_pack.py --platform xhs --pack signal --field json
python tools/social_keyword_pack.py --platform xhs --pack broad --field count
python tools/test_social_keyword_pack.py
```

调试某一天的稳定轮换：

```bash
python tools/social_keyword_pack.py --platform xhs --pack signal --date 2026-08-18 --field json
```
