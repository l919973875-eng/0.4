# 社交平台配置 · 新手版 v0.4

## 0. 先看 v0.4 的诊断结果

现在“工作流跑完”不再等于“平台采集成功”。运行后请看两个地方：

1. 网站 → `系统状态 / Status`；
2. GitHub Actions 本次运行底部 → `Artifacts` → 下载 `social-crawl-diagnostics-*`。

常见状态：

- `ok`：确实产生并导入内容；
- `not_configured`：该平台 Secret 没配置；
- `empty`：程序执行了，但平台返回 0 条；
- `blocked`：检测到验证码、账号无权限、IP/账号风控等；
- `reason_code=account_permission`：常见于小红书 `code=-104`；
- `reason_code=captcha`：GitHub 云端无法人工过滑块；
- `reason_code=ip_block/risk_control`：GitHub Runner 出口或账号触发平台风控。

MediaCrawler 是实验连接器。Cookie 正确也不代表 GitHub 云服务器一定能访问平台搜索接口。


不配置任何社交平台，新闻系统也能正常运行。建议一个个平台加，不要一次全开。

## 1. 小红书 / 抖音 / 微博

进入 GitHub 仓库：

`Settings → Secrets and variables → Actions → New repository secret`

按需要添加：

- 小红书：`XHS_COOKIE`
- 抖音：`DOUYIN_COOKIE`
- 微博：`WEIBO_COOKIE`

Cookie 从你自己已登录的浏览器会话获得，**不要写到公开文件，不要发到 issue，也不要提交到仓库。**

配置后进入：

`Actions → 社交苗头实验采集 · 小红书抖音微博 → Run workflow`

第一次建议只跑一个平台，例如 `xhs`。

如果遇到验证码/风控，Action 会失败或该平台为空。这不影响新闻主系统。

## 2. X

### 方案A：官方 API

添加 Secret：

`X_BEARER_TOKEN`

优先使用这个。

### 方案B：免费实验模式

添加：

`X_COOKIES_JSON`

内容需要是 Twikit 可识别的 Cookie JSON 字典，或浏览器导出的 Cookie 数组。这个模式依赖 X 非官方接口，随时可能因为 X 改版失效，也有账号风控风险。建议使用专门研究账号、小量请求。

## 3. Telegram

添加：

- `TG_API_ID`
- `TG_API_HASH`
- `TG_SESSION_STRING`

然后编辑：

`config/social_sources.yaml`

把公开频道 username 加到：

```yaml
telegram_channels:
  - channel_name_1
  - channel_name_2
```

## 4. 手动临时搜索

进入主 Action：

`Actions → 全球涉华早期信号 · 每日主巡检 → Run workflow`

例如：

- mode：`social`
- query：`Pakistan Chinese CPEC Gwadar`

就会临时把这组关键词加入 X 搜索，不修改你的永久配置。

## 5. 原则

一天自动一次已经足够。遇到突发事件时再手动 Run workflow。

不要把平台抓取量调得很大。你要的是“苗头”，不是把整个平台搬下来。
