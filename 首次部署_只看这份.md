# 首次部署 / 从旧版升级 · 只看这份

## 你已经有 global-china-news 仓库

最简单：把这个压缩包解压后，**把里面全部文件上传覆盖到原仓库**。

不要删除你原来的 `data/articles.json`；它可以继续作为历史数据。

## 然后检查 3 个地方

### 1. GitHub Pages

`Settings → Pages → Build and deployment → Source → GitHub Actions`

### 2. Actions 写权限

`Settings → Actions → General → Workflow permissions`

选择允许工作流 `Read and write permissions`。

### 3. 手动跑一次

`Actions → 全球涉华早期信号 · 每日主巡检 → Run workflow`

第一次：

- mode：`all`
- query：留空
- deep_scan：不勾

成功后进入 Pages 网站。

## 社交平台不是必须

先把新版新闻/事件聚类跑通。之后再看：

`社交平台配置_只看这份.md`

逐个平台配置 Secrets。

## 自动运行时间

主巡检每天北京时间 08:17 一次。遇到特殊事件，你随时可以手动 `Run workflow`，临时输入关键词。
